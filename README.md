# Google-Messages-Tracker
An automation application working with selenium, that stores new messages by using Google Messages.

To run:

```bash
mkdir database && python runner.py
```

Just follow instruction logs at terminal screen.
